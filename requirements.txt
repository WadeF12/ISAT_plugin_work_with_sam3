# ISAT SAM3 Text Prompt Plugin Dependencies
# 
# 基础依赖:
isat-sam>=1.4.0
numpy

# 使用 opencv-python-headless 避免与 PyQt5 的 Qt 平台插件冲突。
# opencv-python (完整版) 自带的 cv2/qt/plugins 会覆盖系统 Qt 插件，
# 导致 "Could not load the Qt platform plugin xcb" 错误。
opencv-python-headless

# SAM3 依赖（根据你的 SAM3 来源选择）:
# 
# 方式1: Meta 官方 sam3 包
# sam3
# torch>=2.0
# torchvision
#
# 方式2: ONNX Runtime (如果使用 ONNX 导出的 SAM3)
# onnxruntime-gpu
#
# 方式3: 如果使用 SAM2 兼容模式
# segment-anything
# torch>=2.0
#
# 请根据实际情况取消注释并安装对应的依赖。
